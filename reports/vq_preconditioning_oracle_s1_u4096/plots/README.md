# Plots intentionally not generated

The required baseline gate failed as a protocol comparison: a one-shot
re-encoding of the FP32 snapshot is not the recursive checkpoint trajectory.
Candidate conditioning was therefore not run, so storage/fidelity plots would
be scientifically misleading. See `comparison.md` and `baseline_protocol_check.json`.
